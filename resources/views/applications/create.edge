@layout('layouts/simple')

@section('home')
<div class="flex items-center min-h-screen p-6 bg-gray-50">
  <div class="flex-1 h-full max-w-4xl mx-auto overflow-hidden bg-white rounded-lg shadow-xl">
    <div class="flex flex-col overflow-y-auto md:flex-row">
      <div class="h-32 md:h-auto hidden md:block md:w-1/2 bg-center">
        <a href="{{ route('home') }}">
          <img class="w-full h-full mg-20 md:w-4/5 z-30 mx-12"
            src="{{ asset('assets/images/create_account.svg') }}" />
        </a>
      </div>
      <div class="flex items-center justify-center p-6 sm:p-12 md:w-1/2">
        <form action="{{ route('users.store') }}" method="POST" class="w-full">
          <h1 class="mb-4 text-xl font-semibold text-gray-700">
            Crie sua conta
          </h1>

          @!form.input({
            label: 'Email',
            placeholder:'Email',
            name: 'email',
            type: 'email',
          })

          @!form.input({
            label: 'Senha',
            placeholder:'Senha',
            name: 'password',
            type: 'password',
          })

          @!form.input({
            label: 'Confirme a sua senha',
            placeholder:'Confirme a sua senha',
            name: 'password_confirmation',
            type: 'password',
          })

          @button({
            type: 'submit',
            block: true
          })
            Criar conta
          @end

          <hr class="my-8" />

          <p class="mt-4">
            <a class="text-sm font-medium text-purple-600 hover:underline" href="{{ route('sessions.create') }}">
              Você já possui uma conta? Faça o login
            </a>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

@endsection
